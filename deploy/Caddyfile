# Automatic HTTPS via Let's Encrypt. DOMAIN and ACME_EMAIL come from the
# environment (set by deploy.sh / docker-compose.prod.yml).
{
	email {$ACME_EMAIL}
}

{$DOMAIN} {
	encode zstd gzip
	reverse_proxy web:3000
}
